import { QuizContainer } from '../Test/QuizContainer';

export const TestPage = () => {
  return (
    <div className="flex justify-between text-sm font-medium">
      <QuizContainer />
    </div>
  );
};
