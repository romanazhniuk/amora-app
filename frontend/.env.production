# Production / GitHub Pages: your deployed API must use HTTPS (avoid mixed-content blocks on https:// pages).
# Replace with your real backend URL (e.g. Render). CI can override via the VITE_API_URL secret.
VITE_API_URL=https://your-backend.onrender.com
