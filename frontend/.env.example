# Optional. Leave empty for local dev: the Vite dev server proxies /api → http://127.0.0.1:8000 (see vite.config.ts).
# Set this when the frontend and API are on different origins (e.g. production SPA + separate API host).
# Example: VITE_API_URL=https://api.example.com
# VITE_API_URL=
